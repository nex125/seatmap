{
  "name": "@nex125/seatmap-root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.58.7",
    "bun-types": "^1.3.13",
    "turbo": "^2.9.6",
    "typescript": "~6.0.3"
  },
  "packageManager": "bun@1.2.4"
}
